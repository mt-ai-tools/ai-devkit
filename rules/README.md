# rules

Coding rules for AI-agent-driven development. Markdown only — nothing here
runs, and nothing here depends on anything.

Adopted all or nothing: they reinforce each other, and some are premises
the rest rest on — marked as such in their frontmatter.

Each rule declares when it becomes decidable, so whatever reads them can raise
each while it can still be acted on rather than only mourned.

{
	"name": "ai-devkit",
	"version": "0.1.0",
	"description": "Rules for a coding agent, the hook that injects them, and the reviewer that enforces them.",
	"private": true,
	"scripts": {
		"test": "bats injector/tests"
	},
	"devDependencies": {
		"bats": "1.13.0"
	}
}
